{
  "name": "promise-protect",
  "version": "1.0.0",
  "files": ["index.js"],
  "description": "Protect promise-returning functions from being called more than once in parallel.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/isaacs/promise-protect"
  },
  "author": "Isaac Z. Schlueter <i@izs.me> (https://izs.me)",
  "license": "ISC",
  "scripts": {
    "test": "tap",
    "snap": "tap",
    "preversion": "npm test",
    "postversion": "npm publish",
    "prepublishOnly": "git push origin --follow-tags"
  },
  "tap": {
    "check-coverage": true
  },
  "devDependencies": {
    "tap": "^14.10.7"
  }
}
